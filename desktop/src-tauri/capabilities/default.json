{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Light Again window",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-close"]
}
