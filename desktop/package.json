{
  "name": "light-again-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Light Again — standalone desktop build (Tauri)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "android:init": "tauri android init",
    "android:dev": "tauri android dev",
    "android:build": "tauri android build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
